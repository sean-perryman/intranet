.container.well
	.col-md-6
		%h1 Editing VM Problem
		= simple_form_for(@vm_problem) do |f|
			= f.error_notification
			= f.input :who_are_you
			= f.input :what_is_the_problem
			= f.input :resolved
			= f.button :submit, "Update Problem"
